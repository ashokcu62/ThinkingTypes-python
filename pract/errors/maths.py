def add(x:int,y:int)->None:

    # in the case of some binary operations sum num orders and zero are will become bug
    # we need to fix those errors thats why we using error fn s here
    print(x/y)

add(25,32)
add(20,0)